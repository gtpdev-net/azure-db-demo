{
  "CosmosDb": {
    "AccountEndpoint": "Overview → URI",
    "DatabaseName": "ExistingDatabaseName",
    "ContainerName": "ExistingContainerName",
    "PartitionKeyPath": "Select ExistingContainer → Scale & Settings → Settings → Partition Key",
    "ConnectionMethod": "ManagedIdentity - For Azure-hosted apps (requires RBAC setup) | ConnectionString - Easiest for development | AccountKey - Alternative for development | Emulator - For local Cosmos DB Emulator",
    "ConnectionString": "Settings → Keys → Primary Connection String",
    "AccountKey": "Settings → Keys → Primary Key"
  }
}
